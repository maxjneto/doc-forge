[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "docforge-mcp"
version = "0.1.0"
description = "MCP server for DocForge — lets AI agents read and write to shared live documents"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0",
    "httpx>=0.27",
    "uvicorn[standard]>=0.30",
    "starlette>=0.40",
]

[project.scripts]
docforge-mcp        = "docforge_mcp.server:main"   # stdio — local dev
docforge-mcp-server = "docforge_mcp.server:serve"  # HTTP  — hosted production

[tool.hatch.build.targets.wheel]
packages = ["src/docforge_mcp"]
